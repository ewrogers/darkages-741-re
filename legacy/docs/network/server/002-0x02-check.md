# 002 / 0x02: Check / Login Check (`SCheck`, lobby)

## Identification

| Property | Value |
|---|---|
| Direction | Server to client |
| Opcode | `0x02` |
| Internal class vocabulary | `SCheck` |
| Related-game enum | `LoginCheck` |
| Local behavioral alias | `LoginResult` |
| Current receive crypto switch | explicitly recognized as primary-key mode |
| Current game-server RTTI registry | not registered |
| Local protocol status | known lobby/login-server packet |

## Payload model

Not yet recovered from a locally registered packet deserializer. The exact login-result variant should be established from a lobby capture or state-specific raw handler.

## Verification status

- Opcode and lobby use: supplied protocol knowledge.
- Internal class vocabulary: leaked related-engine packet template.
- Same-opcode related-game enum: `LoginCheck`.
- Current-client framing/decrypt support: confirmed; the receive switch routes `0x02` through primary-key decryption.
- Support through the currently recovered 61-entry game registry: disproven; there is no `0x02` factory entry.
- Current-client payload fields and exact lobby dispatch path: pending.
