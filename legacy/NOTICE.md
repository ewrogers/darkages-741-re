# Legacy project archive

This directory preserves the repository as it existed before the Binary Ninja reboot on July 15, 2026. It includes the previous documentation, exports, scripts, configuration, and agent instructions.

The archive is not part of the new mdBook and is not a source of confirmed facts. Use it only to find possible strings, addresses, functions, packet handlers, or behaviors to investigate again in the matching version-741 client through Binary Ninja.

Do not continue current documentation inside this directory. New findings belong in the root `docs/`, `analysis/`, and `binaryninja/` directories.
