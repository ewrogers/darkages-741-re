# 006 / 0x06: Move (`CMove`)

- Direction: client to server
- Internal name: `CMove`
- Local behavioral alias: `Walk`
- Related-game enum name: `Move`
- Name provenance: Leaked company/engine class name remapped by this client's local behavior; the sibling-game opcode differs.
- Evidence: 1 concrete builder/sender call site
- Send handling: default common transform mode 1
- Wire frame after transformation: `0xAA, u16be body_size, body`

## Builder call sites

- `net_send_c_move` at `0x5F4580`

## Plaintext body

```text
06 direction:u8 movement_sequence:u8
```

`input_try_move_self` at `0x5F09E0` checks bounds and collision before this sender is called. The sequence at world offset `+0x28A` increments before transmission. World offset `+0x28C` records `timeGetTime()` for the send.
