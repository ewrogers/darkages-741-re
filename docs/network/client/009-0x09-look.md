# 009 / 0x09: CLookPacket?

- Direction: client to server
- Internal packet name: `CLookPacket?`
- Local behavioral alias: `Look / LookAhead`
- Related-game enum name: `Look`
- Name provenance: Related-game internal enum name `Look` at the same opcode; local builder confirmation is still missing, so the C++ class spelling remains reconstructed.
- Evidence: user-researched local command; fixed builder call site not yet isolated
- Send handling: not yet recovered from a concrete builder
- Wire frame after transformation: `0xAA, u16be body_size, body`

## Builder call sites

- Not yet isolated in the current static pass.

## Current structural notes

- Byte 0 of the untransformed body is expected to be opcode `0x09`.
- Payload layout remains to be recovered from a caller or confirmed with a runtime capture.
